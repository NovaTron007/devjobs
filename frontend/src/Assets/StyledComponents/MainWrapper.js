import styled from "styled-components"

const MainWrapper = styled.section`
    max-width: 1160px;
    padding: 0 20px;
    position: relative;
    margin: -110px auto;
    z-index:2;
`

export default MainWrapper